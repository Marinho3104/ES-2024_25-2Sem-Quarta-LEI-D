package tests

import (
	"regexp"
	"testing"
)

func TestReadFile(t *testing.T) {
	type testCase struct {

		// Expected Values
		expected string
		err error
	}

	t.Run("Valid file read values", func(t *testing.T) {})
}
